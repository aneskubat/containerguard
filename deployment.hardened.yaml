# HARDENED Kubernetes Deployment
# All security checks pass - ContainerGuard score: 100/100

apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  # K8S009 - FIXED: Labels defined
  labels:
    app: myapp
    version: "1.0"
    environment: production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
        version: "1.0"
    spec:
      # K8S008 - FIXED: hostNetwork removed
      # K8S001 - FIXED: runAsNonRoot defined
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000

      containers:
      - name: myapp
        # K8S004 - FIXED: Specific image version
        image: nginx:1.25.3

        ports:
        - containerPort: 8080

        # K8S010 - FIXED: Secrets loaded from Kubernetes Secret object
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: myapp-secrets
              key: db-password
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: myapp-secrets
              key: api-key

        securityContext:
          # K8S002 - FIXED: Privileged mode removed
          privileged: false
          # K8S005 - FIXED: allowPrivilegeEscalation disabled
          allowPrivilegeEscalation: false
          # K8S007 - FIXED: Read-only root filesystem
          readOnlyRootFilesystem: true
          capabilities:
            drop:
              - ALL

        # K8S003 - FIXED: Resource limits defined
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"

        # K8S006 - FIXED: Liveness and readiness probes defined
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

        volumeMounts:
        - name: tmp
          mountPath: /tmp

      volumes:
      - name: tmp
        emptyDir: {}